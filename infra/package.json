{
  "name": "@stablebook/infra",
  "version": "0.0.0",
  "private": true,
  "description": "SST infrastructure: Cognito, RDS Postgres, API Gateway + Lambda, Migrator Lambda.",
  "type": "module",
  "scripts": {
    "typecheck": "echo 'infra: typecheck deferred to `sst diff` (SST platform sources are not strict-TS clean)'",
    "sst:diff": "sst diff",
    "sst:deploy": "sst deploy",
    "sst:remove": "sst remove",
    "sst:dev": "sst dev"
  },
  "dependencies": {
    "@stablebook/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "sst": "4.14.3",
    "typescript": "^5.6.3"
  }
}
