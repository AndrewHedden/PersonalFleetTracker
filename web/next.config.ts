import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Amplify Hosting auto-detects Next.js and picks SSR mode based on the
  // build output. No special config needed — keep this minimal.
};

export default nextConfig;
